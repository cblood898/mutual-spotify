extends layout

block content
  #app(data-remember=remember).viewportHeight
  - src = process.env.NODE_ENV === 'production' ? '/application.js' : 'http://localhost:8080/application.js'
  script(src=src)
